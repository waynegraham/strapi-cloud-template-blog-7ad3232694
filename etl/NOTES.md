# TODO

- consolidate multiple IAB Codes to the first item
- Split `display_date_en` and `display_date_ar` 
    - `DateDisplayGregorianEn` `DateDisplayGregorianAr`
    - `DateDisplayHijriEn` and `DateDisplayHijriAr`
- Extract `earliestDate` and `latestDate` parsing `DateDisplayGregorianEn`
- Convert Markdown in `*description` fields to HTML
- Link `*description` to `*footnotes`
    - Reset count? 

## Image Renaming

- `ln -s oldname newname` is this possible in S3 storage?