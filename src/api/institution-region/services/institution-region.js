'use strict';

/**
 * institution-region service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::institution-region.institution-region');
