'use strict';

/**
 * institution-region router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::institution-region.institution-region');
